
# Реальна адмін-статистика GG RUSH

Ця версія додає в адмінку:
- 🟢 реальний онлайн за останні 90 секунд;
- 👥 всіх гравців;
- 🆕 нових сьогодні;
- 👆 тапів сьогодні;
- 💰 загальний GG;
- 🎁 відкритих кейсів;
- 👥 рефералів;
- 🏆 реальний топ;
- 📈 онлайн за 24 години;
- 🔄 оновлення кожні 10 секунд.

## Потрібно налаштувати

1. У `index.html`:
   - `OWNER_TELEGRAM_ID` = твій числовий Telegram ID;
   - `ADMIN_API_URL` = URL твого Cloudflare Worker.

2. У `worker/schema.sql` створи таблиці в D1.

3. Worker має бути прив'язаний до D1 як `DB`.

4. У Worker задай `OWNER_TELEGRAM_ID`.

5. **Важливо:** для production потрібно перевіряти Telegram `initData` через HMAC з BOT_TOKEN на сервері. Не покладайся лише на ID, який прийшов з браузера.

Якщо база не підключена, адмінка НЕ вигадує статистику — показує OFFLINE/0.
